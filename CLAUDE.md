# 123Pomadoro

## Versioning
- Current app version is tracked in `VERSION` using semantic versioning.
- Every commit should bump the version in `VERSION` using semver.
- Keep `application.fam` `fap_version` aligned to the same major/minor version.
- Every version commit should also create a matching git tag in the form `vX.Y.Z`.
